import { Button } from '@/components/ui/button'
import { UserButton } from '@clerk/nextjs'
import React from 'react'

const Homepage = () => {
  return (
    <div> lets build a mockup 
<Button> lets go</Button>
      <UserButton/>
    </div>
  )
}

export default Homepage